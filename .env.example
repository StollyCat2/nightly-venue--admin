# Admin email — must match the Firebase Auth email for the Nightly admin account.
# This email gets the admin panel; all other logins see the venue dashboard.
# NOTE: Also update firestore.rules if you use a different admin detection strategy.
EXPO_PUBLIC_ADMIN_EMAIL=admin@nightly.no
